{
  "mcpServers": {
    "claude-knowledge-base": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp/server.js"],
      "env": {
        "DATA_DIR": "${PROJECT_ROOT}/.bluera",
        "CONFIG_PATH": "${PROJECT_ROOT}/.bluera/config.json"
      }
    }
  }
}
